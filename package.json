{
  "name": "redencionlauncher",
  "version": "1.0.2",
  "description": "Launcher para RedM",
  "main": "src/main.obf.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . RedencionLauncher --platform=win32 --arch=x64 --icon=favicon.ico --out=build --overwrite"

  },
  "devDependencies": {
    "electron": "^25.3.0",
    "electron-builder": "^26.0.12",
    "electron-packager": "^15.5.2"
  }
}
